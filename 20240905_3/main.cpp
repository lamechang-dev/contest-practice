#include <bits/stdc++.h>
#include <iostream>
using namespace std;

int main()
{
    int n;
    vector<bool> n_status;
    vector<int> s;
    vector<int> t;

    for (int i = 0; i < s.size(); i++)
    {
        cin >> s[i];
    }

    for (int i = 0; i < t.size(); i++)
    {
        cin >> t[i];
    }
}